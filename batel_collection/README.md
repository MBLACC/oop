
# Batel Collection Website

This is the source code for the Batel Collection E-commerce Web Application, built with [Next.js](https://nextjs.org) and [Tailwind CSS](https://tailwindcss.com).

## 🚀 How to Run the Application

Follow these steps to start the website and view it in your browser.

### Step 1: Open Terminal
Open your terminal (PowerShell, Command Prompt, or Git Bash) and navigate to this folder:
```bash
cd "c:\Users\B.L.A.C.C\OneDrive\Pictures\PROJECTS_WEB 2\batel_collection"
```

### Step 2: Install Dependencies
If you haven't done so already, verify all required tools are installed:
```bash
npm install
```
*Note: This might take a few minutes if the internet connection is slow.*

### Step 3: Start the Development Server
Run the following command to turn on the website:
```bash
npm run dev
```

### Step 4: View in Browser
- The application is configured to **automatically open** your default browser to `http://localhost:3000`.
- If it doesn't open automatically, look for the link in the terminal (e.g., `http://localhost:3000`) and hold **Ctrl** and **Click** it, or type it manually into Chrome.

## 📂 Project Structure

- **`/app`**: Contains all the pages (Home, Shop, Cart, Checkout, etc.).
- **`/components`**: Reusable UI parts like Navbar, Footer, and Product Cards.
- **`/public/images`**: Your product images and assets.
- **`/lib`**: Utility functions and mock data.

## 🛠 Features

- **Home Page**: Featured products and categories.
- **Shop**: Filterable product listing.
- **Product Details**: Full view of product info.
- **Cart & Checkout**: Complete shopping simulation.
- **Admin Dashboard**: View sales stats.

## 🛑 Stopping the Server
To stop the website, go back to your terminal window and press **Ctrl + C**.
